#include<iostream>

using namespace std;

int main()
{
    int a,b;
    cout << "\nDigite o valor de a ";
    cin >> a; // >> leva a inforamação para o "a" (leia)
    cout << "\nDigite o valor de b ";
    cin >> b;
    cout << "\nA soma de " << a << " com " << b << " = " << a+b;
    return 0;
    
}